# Evidence Capture 실행 로그

## 2026-08-20

### 완료

- Google 공식 사용자 문서 기준 기능 목록과 절차 확인
- `capture-index.md`에 단계별 계획, 계정 유형, 공식 출처, 슬라이드 용도 등록
- 기능별 `captures/` 디렉터리 구성
- 실제 학생 데이터 대신 사용할 가상 과제·루브릭·Notebook 소스·UI 입력 문구 작성
- New Gems from Google Labs가 공식 문서상 개인 계정 전용임을 사전 판정
- Spark가 공식 문서상 개인 계정·성인·지원 구독·지역·Keep Activity 조건을 요구함을 사전 판정
- 웹 슬라이드 제작은 Evidence Capture 완료 뒤 시작하도록 게이트 설정
- GitHub 연결은 캡처 개인정보 검수 후, 웹 슬라이드 개발 전에 수행하도록 전환 지점 설정
- Windows용 Chrome과 별도 교육/개인 프로필을 사용하는 Playwright Evidence Capture 하네스 구성 및 공개 페이지 자체 시험 완료
- 교육용 계정 로그인과 Classroom 교사 뷰 확인
- Gemini in Classroom의 진입 → 입력 → 생성 → 검증 → Sheets 내보내기 → 게시 전 자료 초안 단계를 실제 UI로 캡처
- 자료 초안은 `저장됨` 상태만 확인하고 `게시`를 실행하지 않음
- 교육용 계정에서 Classic Gem `수행평가 루브릭 점검관 (연수용)`의 지침 입력 → 미리보기 → 저장을 완료하고 공유는 실행하지 않음
- Classroom에서 교육용 Gemini Notebook을 만들고 가상 소스 추가 → 소스 한정 질문 → 인라인 인용 대조 → 마인드맵 생성 → Classroom 자료 초안 확인까지 완료
- Notebook이 첨부된 Classroom 자료는 `저장됨` 상태만 확인하고 `게시`하지 않음
- 교육용 계정에서 Workspace Studio 자연어 Flow 생성 → Starter/Steps 확인 → 변수 연결 확인 → 안전한 Test run → 꺼진 최종 상태 확인을 완료
- 자동화 로그인이 거절된 개인 계정 격리 프로필을 일반 Chrome으로 열어 사용자가 직접 로그인한 뒤, 같은 프로필을 캡처 하네스에서 재사용해 로그인 세션을 이어감
- 개인 계정에서 New Gems from Labs의 진입 → 자연어 요구 입력 → workflow 생성 → 두 합성 입력 → 네 항목 분석 결과 확인을 완료
- New Gems의 Opal 권한 안내는 사용자가 직접 `Accept`를 눌러 진행했고, 공유·게시·다운로드는 실행하지 않음
- 개인 계정에서 Spark 첫 사용 동의와 확인을 사용자가 직접 완료한 뒤, Spark 홈·Skills·Task 실행을 실제 UI로 검증함
- `루브릭-근거-점검-연수용` Skill을 수동 생성하고 `/` 메뉴로 합성 루브릭 Task에 적용해 진행 중 → 완료 → 결과 단계를 캡처함
- `captures/` PNG 56개를 전수 검수해 실제 학생 개인정보·실제 성적·계정 이메일이 없음을 확인하고, 표시명·개인화 Gem 목록이 있는 5개 원본의 공개본 처리 원칙을 `privacy-review.md`에 기록함

### 실제 UI 차이

- 루브릭 입력에서 `4·3·2·1점, 총점 16점`을 명시했지만 생성 결과와 내보낸 Sheets에는 숫자 배점이 반영되지 않음
- 4수준 이름이 `초과 / 회의 / 접근 / 접근하지 않음`으로 표시됨. 문맥상 `회의`는 부자연스러운 한국어 번역임
- 생성 문장에 `완벽하게`, `전문적`, `성실하게` 등 직접 관찰하기 어려운 표현이 포함되어 교사 검토·수정이 필요함
- `수업에 추가`에서 Sheets·수업·자료를 선택하고 확인하면 먼저 Sheets를 생성한 뒤 `저장됨` 상태의 자료 작성 대화상자가 열림. 게시 동작은 별도 버튼으로 분리됨
- 한 줄 프롬프트는 과제 설명 없이 범용 평가 기준을 임의 구성함
- 역할·작업·맥락·형식을 넣은 구조화 프롬프트는 형식 준수도가 높아졌지만, 레이아웃 스케치·그래픽 모티프·학습 완료 상태 등 입력에 없는 맥락을 여전히 추정함
- 연수용 Harness에서 입력 범위·금지·결측 처리·검증을 명시하자 근거 없는 중간 수준을 `확인 필요`로 남기고 결측 목록과 자체 점검표를 출력함
- 연수용 Verification Loop에서 선택 근거만 `검증 가능`, 타깃·위계는 `모호함`, 완성도는 `근거 부족`으로 재분류하고, 수정본에서 근거 없는 내용을 제거함
- Classic Gem 미리보기는 합성 루브릭의 누락 조건, `창의성·성실성` 같은 관찰 불가능 표현, 점수 구조 부재, 교사가 결정할 항목을 구분해 출력함
- Gem 편집 화면의 지침 보강 동작이 입력 중 실행되어 지침이 확장되었으나 정확한 실행 계기는 캡처만으로 확정하지 않고 기록에서 자동 동작으로 단정하지 않음
- Notebook 답변은 원문에 있는 제출물 3개·평가 항목 5개를 인용과 함께 표로 정리하고, 원문에 없는 총점과 제출 기한은 `명시 없음`으로 처리함
- 인용 번호를 클릭하면 `인용 세부정보`에서 해당 소스 원문이 열려 답변과 근거를 같은 화면에서 대조할 수 있었음
- 마인드맵 생성 전 사용자 지정 범위와 금지 조건을 입력할 수 있었고, 결과는 `과제 개요 / 제출물 세 가지 / 평가 항목 다섯 가지 / 교사 검증 원칙`으로 구성됨
- Classroom에서 만든 Notebook 상단에는 `공유됨` 상태가 보였으나, 실제 학생 배포는 Classroom 자료의 별도 `게시` 동작으로 남아 있었고 실행하지 않음
- Workspace Studio는 자연어 요구에서 `매주 월요일 9시` Starter와 두 Ask Gemini 단계를 정확히 생성하고, 3단계에 `2단계: Gemini에서 생성한 콘텐츠` 변수 칩을 자동 연결함
- 두 Ask Gemini 단계의 기본 소스 설정은 웹 검색과 Workspace 콘텐츠 접근이 모두 켜져 있었으므로 테스트 전에 둘 다 꺼 고정 가상 입력만 사용하도록 제한함
- Test run 화면은 실제 메시지 전송·파일 업데이트·회의 설정 가능성을 명시적으로 경고함. 외부 앱 단계가 없는 내부 Gemini 전용 Flow만 실행했고 `실행 완료됨`을 확인함
- Flow는 테스트 후에도 `사용 설정` 버튼 상태로 남아 있어 예약 실행을 켜지 않음
- 교육 계정 Gem 관리자에는 `Google 사전 제작`과 `내 Gems`만 표시되고 `Gems from Labs` 영역은 노출되지 않음
- Spark 공식 문서는 work/school 계정 미지원이라고 안내하지만, 교육 계정에서 `Ctrl+Shift+S`를 실제 입력하자 `/spark`의 베타 첫 사용 안내가 열림
- Spark 안내에는 연결 앱·로그인 웹사이트·위치 등의 정보 사용, 서드파티 공유, 원격 브라우저 데이터 저장, 감독 필요 위험이 표시됨. 법적·개인정보 동의 성격의 `계속`은 대신 누르지 않고 `나중에`로 닫음
- 따라서 교육 계정 Spark 판정은 `완전 사용 가능`이 아니라 `공식 문서와 달리 진입·온보딩 노출, 동의 후 실제 사용 여부 미확정`으로 기록함
- 개인 계정 Gem 관리자에는 교육 계정에 없던 `Gems made by Labs`, `My Gems from Labs`, `New Gem` 영역이 노출됨
- New Gems 생성 과정에서 Opal은 Gemini Apps와 별도 서비스이고 상호작용 기록을 Google Drive에 저장하며, 사용한 특정 Drive 파일에 대한 생성·편집·삭제 권한을 요구한다는 안내가 표시됨. 동의는 사용자가 직접 수행함
- 요청한 앱 제목 `수행평가 루브릭 점검 실험실 (연수용)`은 Gem 목록/편집기에서 `루브릭체크`로 축약됐지만, 생성된 미니앱 내부 제목은 원문을 유지함
- New Gems는 `Task Requirements → Rubric Draft → Analyze Rubric Draft → Generate Analysis Report` 4단계를 생성함
- 합성 실행 결과는 요구 기준 누락, `매우 창의적·성실하게 수행`의 관찰 가능성 부족, 요구 총점 16점과 초안 총점 20점의 불일치를 정확히 구분함
- `교사가 결정할 항목`에는 입력에 없던 학년·기한·제출 방법을 추가 정의 대상으로 제안했으므로, 이는 근거 사실이 아니라 사람이 결정할 결측 후보로 설명해야 함
- Spark Skill 이름은 입력한 공백·괄호 형식에서 `루브릭-근거-점검-연수용`으로 자동 정규화됨
- `/`를 실제 키 입력했을 때 생성한 Skill이 메뉴에 나타났고, 선택 후 프롬프트 안에 굵은 Skill 칩으로 삽입됨. 단순 문자열 입력만으로는 메뉴가 열리지 않아 실제 키 이벤트가 필요했음
- Spark Task는 전용 `/spark/chat/...` 화면과 작업 목록을 만들고 `Initializing task / 진행 중 / 사고하는 중…`을 거쳐 `완료`로 변경됨
- 완료 결과는 Skill 지침에 맞춰 확인된 요구사항, 누락, 관찰하기 어려운 표현, 배점·총점 불일치, 교사 결정, 검증 체크리스트와 최종 교사 대조 경고를 출력함
- Task 본문과 Skill 지침에서 외부 앱·개인 인텔리전스·로그인 웹사이트·파일 생성·공유·발송·일정 생성을 금지했으며, 실제 실행에서도 해당 외부 동작은 발생하지 않음

### 우회 완료된 차단

- 상태: `blocked-computer-use`
- 대상: Codex Settings → Computer Use → Google Chrome / Microsoft Edge
- 직접 확인된 UI: 두 브라우저 토글이 비활성화되어 있고 `조직에서 비활성화했거나 해당 지역에서 사용할 수 없습니다`라고 표시됨
- 추가 오류: Windows Computer Use 창 목록 조회 시 `sandboxCwd is not a local file URI: file:///mnt/c/...`가 재현됨
- 판정: 브라우저 프로필이나 Google 계정의 문제가 아니다. Codex의 브라우저 제어 기능 자체가 조직 정책 또는 지역 조건으로 비활성화된 상태다. 화면만으로 두 원인 중 어느 것인지는 구분할 수 없다.
- 공식 OpenAI 문서 확인: Computer Use는 지원 지역에서 제공되며, 조직/관리 정책으로 기능을 비활성화할 수 있다. Windows에서는 기능이 허용된 뒤 대상 앱을 활성 데스크톱에 표시해야 한다.
- 우회: Codex에 번들된 Windows Playwright 런타임과 전용 Chrome 프로필을 사용해 단계별 UI 확인·캡처를 수행 중이다. Computer Use 설정은 변경하지 않았다.

### 개인 계정 로그인 차단 및 우회 완료

- 개인 계정 전용 Playwright Chrome에서 Google 로그인을 시도하자 `로그인할 수 없음`과 `브라우저 또는 앱이 안전하지 않을 수 있습니다`가 표시됨
- 계정 보안 설정 오류로 단정할 근거는 없으며, 자동화 표시가 있는 브라우저 세션을 Google 로그인에서 거절한 것으로 판정함
- 일반 Chrome에서도 다시 시도하라는 화면 안내에 따라 동일한 격리 프로필을 자동화 표시가 없는 일반 Chrome으로 열어 사용자가 직접 로그인하도록 전환함
- 일반 Chrome 로그인 후 창을 닫고 같은 격리 프로필을 캡처 하네스로 다시 열어 세션 쿠키가 유지되는 것을 확인함

### 다음 실행 순서

1. 파일명과 `capture-index.md` 일치 및 planned/verified 상태 최종 검수
2. 비공개 GitHub 저장소 연결과 첫 커밋
3. 100분 강의 구조와 캡처 매핑을 확정해 HTML/CSS/Vanilla JS 웹 슬라이드 개발 시작
4. URL hash·키보드·전체화면·16:9 print CSS와 PDF 출력 검증

## GitHub 전환 지점

아직 GitHub 저장소를 만들거나 연결하지 않는다. 아래 조건이 충족되면 비공개 저장소로 연결한다.

- 1차 Evidence Capture 파일 확보
- 실제 학생/계정 개인정보 노출 검수 완료
- 파일명과 `capture-index.md` 일치 확인
- 캡처 원본과 슬라이드용 편집 요소 분리 확인

그 후 문서·가상 데이터·검수된 캡처를 첫 커밋으로 저장하고 HTML/CSS/Vanilla JS 웹 슬라이드 개발을 시작한다.
